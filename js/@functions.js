$(document).ready(function() {
	
	/*$("legend").each(function() {
		var text = $(this).text();
		text = text.replace("a", "ADD");
		$(this).text(text);
	});*/
	
	//$('#menu h3:first').css('background', 'none');
	
	$('.sideBarDepartmentNavigator .menu-departamento .brandFilter').appendTo('#sideBar #brandsList');
	
	/*
	$('.search input.fulltext-search-box').val('');
	$('.search input.fulltext-search-box').click(function() {
		if($(this).val() == '') {
			$(this).val('');
		}
	
        $('.search input.fulltext-search-box').blur(function() {
            if($(this).val() == '') {
               $(this).val('');
            }else if ($(this).val()== 'Digite Aqui'){
                $(this).val('');
            }
        });
    });
	*/
	
});